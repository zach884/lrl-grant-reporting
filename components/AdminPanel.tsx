// components/AdminPanel.tsx — Admin-only sync controls (Phase 2 placeholder)
// TODO Phase 2: Manual backfill, config cache refresh, visible to admins only
export default function AdminPanel() {
  return <div>AdminPanel — Phase 2 placeholder</div>;
}
