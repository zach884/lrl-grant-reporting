// lib/wix-sync/index.ts — barrel for the outbound GHL -> Wix sync engine.

export { syncContactToWix, resolveContactField } from './sync';
export type { WixSyncResult, WixFieldChange } from './types';
